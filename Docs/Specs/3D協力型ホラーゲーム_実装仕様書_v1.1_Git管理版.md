# 3D協力型ホラーゲーム 実装仕様書 v1.1（Git管理版）

> **目的**：デバフを持つ2〜4人のプレイヤーが、互いの欠点を補いながら玩具店から換金品を回収し、敵を避けて脱出するゲームプレイが面白いかを検証する。
>
> **この文書の優先度**：本文に数値・挙動がある場合はそれを採用する。未記載事項は「実装しない」。追加案は `Future` に記録し、勝手に実装しない。

## 0. 固定する前提

|項目|決定|
|---|---|
|ジャンル/視点|協力型・一人称ホラー（FPS）|
|対応人数|1〜4人。評価の主対象は2〜4人|
|試作マップ|固定マップ「閉店した巨大玩具店」。ランダムなのはスポーンのみ|
|ゲームの主軸|アイテム回収、相互補完型デバフ、敵からの逃走。能力スキル/成長ツリーは作らない|
|エンジン|Unity 6 LTS、URP、C#|
|ネットワーク|Netcode for GameObjects (NGO) + Unity Transport。Host Authority|
|バージョン管理|Git必須。`main`は常にビルド可能な安定版、実装はPhase単位のfeature branchで行う。Unity Asset SerializationはForce Text、Meta FilesはVisible|
|入力|Unity Input System。キーボード/マウス必須、ゲームパッドはFuture|
|言語|ja（既定）、en、zh-Hans、ko|
|目標PC|High: Ryzen 7 5700X / RTX 3080 10GB / RAM 32GB、2560×1440 High 平均60 FPS以上|
|非目標|プロシージャル生成、スキル、永続進行、ボイスチャット、マッチメイク、アンチチート、複数マップ|

## 1. 既存案のレビューと解決方針

|論点|問題|確定判断|
|---|---|---|
|「協力」の意味|単なる人数増しだと成立しない|各デバフは必ず他者の**操作**で緩和できる。単純な速度/HP低下のみは禁止|
|ソロ対応|協力専用設計だとテスト不能|ソロでは各デバフに低効率の代替手段を与え、同時に有効デバフは1つへ制限|
|物理同期|全Rigidbody同期はジッター/帯域が重い|重要アイテムのみServer/Host所有。停止物はスリープ化。共同運搬は物理拘束でなく権威側のKinematic追従|
|敵数|複数AI/複雑な演出は検証を遅らせる|マネキン1、コレクター1を上限。各タイプは同時1体|
|死亡|即脱落は協力検証を断つ|ダウン→救助→死亡（幽霊観戦）にする。死亡者の蘇生は無し|
|ランの終わり|「回収量」と「脱出」が曖昧|目標額達成後に出口ゲート解錠。生存者がゲートに入ると個別脱出、全員死亡/制限時間で終了|
|グラフィック|LowとLaptopの差が曖昧|Laptopは見た目だけを下げ、敵検知距離、扉、アイテム、霧による遮蔽判定は全プリセット同一|
|設定破損|起動不能の恐れ|検証済み安全設定で起動、`Reset Graphics` は保存値も消して既定値に戻す|
|スコープ|共同運搬・死体運搬・踏み台を全部高品質化すると過剰|MVPは共同運搬、蘇生、Ping、受け渡し、ドア保持、照射。掴む/踏み台/死体運搬はVertical Slice後半|
|変更管理|Codexの大規模変更で差分追跡・巻き戻しが困難|GitでPhase/機能単位にbranchとcommitを分離。テスト失敗状態を`main`へ入れない。履歴破壊コマンドは禁止|

## 2. スコープ分離

### MVP（最初に動作を確認する最小版）

- 1人ローカル + 2人Host/Client接続、固定灰箱マップ、移動・視線・Interact
- 6種類の通常アイテム、単独/2人共同運搬、回収・売却ゾーン、目標額、脱出、リザルト
- デバフ4種（`Tremor`, `TunnelVision`, `HeavyBreath`, `FragileGrip`）をランダムに各人1つ付与
- Ping、ライト、ドア保持、ダウン/救助、死亡、マネキン1種
- 日本語UI、設定保存、Laptop/Low/Medium/High/Custom、Reset/Apply/Revert

### Vertical Slice（友人4人で面白さを評価する版）

- 本仕様の玩具店、通常10種＋呪物4種、デバフ10種、敵2種
- 2〜4人、ロビー、4言語、全HUD/リザルト、切断の最低限処理
- 支える、照射、受け渡し、死体運搬。踏み台と掴むは簡略版
- 音響/ノイズ、照明、隠れ場所、ランダムスポーン、全テスト計画

### Future（試作で作らない）

- Steam認証・ロビー検索・途中参加・ホスト移譲・専用サーバー・クロスプレイ
- マップ複数、完全自動生成、恒久的なレベル/スキル/ショップ、実績
- ボイスチャット、リプレイ、MOD、チュートリアル演出、複数敵の同時出現
- 高度なIK/ラグドール、破壊可能環境、ゲームパッド完全対応、写真モード

## 3. ランのゲームループ（Data Driven 初期値）

`Lobby → デバフ抽選 → 店舗侵入 → 探索/補完/回収 → 売却カートへ搬送 → 目標額到達 → 出口解錠 → 脱出 → Result`

|項目|初期値|仕様|
|---|---:|---|
|1ステージ|12分|残り2分でBGM/照明を警戒状態に。延長なし|
|1ラン|12〜16分|ロビー・リザルト除く。将来は複数ステージに拡張可|
|アイテム|通常14〜18、呪物1〜2|スポーン表から抽選。固定Spawn Pointに重複なし|
|回収目標|$1,500|売却カートへ**持ち込み**済みの価値のみ加算|
|脱出条件|目標達成後、出口ゲート解錠。生存者が出口内3秒滞在|脱出者は再入場不可。未脱出生存者は残り時間まで続行可|
|成功|1人以上が脱出し、売却済み額が目標以上|チーム成功。脱出人数を記録|
|失敗|制限時間0、または生存者0|獲得金額0、統計だけ表示|
|ダウン|攻撃1回でダウン|60秒以内に救助されなければ死亡。敵に再攻撃されると即死亡|
|死亡|観戦状態|操作/復活不可。死体と所持品を残す|
|アイテムロスト|未売却品は成功しても0、売却済みのみ確定|死亡/切断では所持品をその地点に落とす|
|仲間回収|死亡者の「社員証」($150)をカートへ持ち帰る|結果画面に回収済みとして記録。生存報酬ではなく探索判断を作る|
|Result|成功/失敗、売却額、目標、脱出者、死亡/回収、デバフ補助回数、敵遭遇、破損額|ランク・経験値は出さない|

すべての数値は `RunConfig` ScriptableObject と JSON override で持つ。コードに金額・秒数・確率を直書きしない。

## 4. プレイヤー仕様

|項目|値/判断|
|---|---|
|移動|Walk 4.2m/s、斜め入力は正規化。空中操作0.2|
|ダッシュ|あり。6.0m/s、スタミナ100、消費20/s、回復16/s（停止/歩行時）。重量物/共同運搬/支える間は不可|
|ジャンプ|あり。高さ1.0m、連続ジャンプ不可、着地硬直0.10秒|
|しゃがみ|あり。高さ1.75→1.10m、速度2.4m/s。遮蔽判定に使用|
|HP|数値HPなし。`Alive / Downed / Dead / Escaped` の状態機械|
|落下|3.5m未満無効、3.5〜6mでDowned、6m超でDead。試作マップは致死落下を作らない|
|Interact|中心レイ3.0m、角度15°、優先表示1対象。E長押し0.25秒（救助のみ2.0秒）|
|保持|片手アイテムは前方0.9m固定、最大重量15kg。両手は前方1.1m、移動3.0m/s|
|投擲|片手かつ投擲可のみ。右クリック長押し0〜0.7秒、初速5〜10m/s。呪物/両手/共同運搬中は不可|
|Ping|Q、最大30m、6秒表示、個人CD2秒。対象または地面へ。敵へPingしても敵位置を透視しない|
|ライト|F、円錐35°、15m。電池無限。オン中はAI向けノイズ0、視認補正なし|
|当たり|プレイヤー同士は物理衝突しない（push不可）。共同操作中だけ互いの通過を禁止しない|

## 5. デバフ仕様（Vertical Sliceは各プレイヤー1つ、重複可）

制限共通：画面を反転/入力不能にしない、点滅は3Hz未満、色だけで情報を伝えない、操作不能は最大2秒、ソロ補助は常に存在。`Synergy` は同一人物に2デバフを積むFuture用のメモで、試作では複合付与しない。

|ID / 名称|効果・数値|本人への影響|仲間による補完|ソロ代替|相性 / 不快制限|
|---|---|---|---|---|---|
|`Tremor` 手の震え|保持揺れ±0.18m、Fragility耐久消費×1.8|壊れ物を安全に運べない|2人共同運搬で揺れ±0.04m、仲間が受け取る|床に置いて0.8秒で安定化|`FragileGrip` と強い。視点は揺らさない|
|`TunnelVision` 視野狭窄|画面端20%を暗化、Ping表示距離12m|周囲・出口を見失う|ライト照射中の味方/ Pingは全距離で強調|手持ち地図を3秒開く（移動不可）|`LostVoice` と強い。暗化は透明度65%上限|
|`HeavyBreath` 息切れ|ダッシュ消費35/s、回復8/s、呼吸ノイズ半径8m|逃走で敵を呼ぶ|味方が「支える」間、ダッシュ扱いで4.6m/s・ノイズ半径3m|しゃがみ静止2秒でスタミナ50回復|`HearingLoss` と相補。音量は設定別、視覚メーター併記|
|`FragileGrip` 滑る手|保持中、被弾/ジャンプで25%落下|重品・投擲が危険|仲間がアイテムを掴む/共同運搬中は落下無効|テープ箱をInteract（2秒、ラン1回）で落下率0%を45秒|`Tremor` と強い。ランダム落下は0.75秒予告アイコン|
|`HearingLoss` 難聴|敵の足音/味方Ping音を-18dB。左右定位なし|音による察知不可|仲間のライト点滅、Ping視覚マーカー、肩を掴んで誘導|字幕型「危険振動」HUD（敵8m以内のみ）|`TunnelVision` と辛いので同時付与禁止。重要音に視覚補助|
|`LostVoice` 声が届かない|自分のPing半径10m、救助要請Ping CD4秒|離れた仲間に知らせにくい|仲間のPingを受けると自分のHUDで方向線を表示|設置ビーコン1個（20m、60秒）|ボイスチャット非依存。沈黙を強制しない|
|`LowLight` 暗順応不良|ライトOFFの環境でInteract距離1.5m、敵シルエット不可|暗部探索できない|味方ライトに入ると通常化。ライトを向けてもらう|壁の非常灯の近くでは通常化|画面を真黒にしない。最低環境照度を確保|
|`BackPain` 腰痛|重量8kg超で歩行2.5m/s、投擲不可|重い物を運べない|共同運搬で両者3.4m/s、支えると3.0m/s|重量8kg超は引きずり（1.8m/s、ノイズ8m）|`HeavyBreath` と同時付与禁止|
|`StaticFear` 静電恐怖|呪物を持つ間、0.7秒毎にノイズ半径4m。置けば停止|呪物回収が危険|味方が1.5m以内で支えるとノイズ半径1m|絶縁ケース（マップ固定1個）に入れて運搬|視覚ノイズなし、音量ピークなし|
|`Balance` 平衡不良|横移動/旋回時に2秒、速度-25%。直進は通常|狭所・追跡で遅い|味方が掴んで誘導中は無効|壁沿い移動（壁0.5m内）で無効|カメラロールなし、酔いを起こさない|

**抽選**：4人ではこの表から無作為、ただし禁止組合せを再抽選。MVPは先頭4種。将来、デバフごとに個別の治療/解除は作らない。

## 6. 協力アクション共通規約

- すべてHostが距離・状態を検証して開始/終了を決定する。Clientは要求のみ送る。
- 表示は対象のアウトライン、操作キー、必要人数/進捗、相手名（匿名ならPlayer 1〜4）。
- 専用の長尺アニメーションは不要。試作は一人称手元＋三人称用の簡易IK/ポーズでよい。判定はアニメーションに依存させない。

|機能|操作/条件|同期・物理|終了/キャンセル|UI/演出|
|---|---|---|---|---|
|共同運搬|両者が2m以内で同じ大型物にE。`RequiredCarriers=2`|HostがアイテムをKinematic化し、2人の中点+前方に追従。両者位置/状態のみ同期|どちらかがE、距離3m超、Downed、切断|2/2、運搬者名、重さ。手がハンドルを持つ|
|支える|Downed以外の味方の背後1.2mでE押下|対象はHostが移動入力を受け、補助者は追従。Rigidbodyなし|E、距離1.8m、攻撃、しゃがみ|鎖アイコン。2人で肩を支えるポーズ|
|ドアを押さえる|開いたドアの保持側でE長押し|HostがDoorをOpenHeld固定。物理ヒンジは停止|E離し、Downed、距離外|保持ゲージ。ドア開度は全員同期|
|ライト照射|Fで点灯、対象を照らす|ライトON/方向を10Hz同期。判定はHostの円錐判定|OFF、距離外、遮蔽|被照射者にライト輪郭。実ライトとゲーム判定を分離|
|Ping誘導|Qで対象/地点|HostがPing ID・位置・有効期限を配信|6秒経過、同時最大3|色+アイコン+方向線|
|掴む/誘導|味方1.2mでE（`Balance`等のみ）|Hostが相対位置を追従、衝突無効|E、距離、ダメージ|手のアイコン。MVP外|
|踏み台|味方の前1mでしゃがみ+E、もう一人が上をE|Hostが高さ0.65mのKinematic台を生成。プレイヤー物理積みは使わない|離す、5秒、攻撃|上昇矢印。Vertical Slice後半|
|受け渡し|保持中、味方2m以内でE|Hostが瞬時に所有者変更、落下なし|相手枠なし/相手Downed|「渡す Player」表示、手渡しポーズ|
|死亡者運搬|死体1.5mでE。1人可、重品は不可|死体はKinematicで後方追従。ラグドール同期なし|E、敵攻撃、距離2.5m|ドラッグ表示。Vertical Slice|
|救助|Downed味方1.5m、E長押し2秒|Hostが進捗を管理。救助者被弾で中断|距離、被弾、60秒切れ|全員に救助ゲージ、救助後3秒無敵なし|

## 7. データ構造

### ItemDefinition（ScriptableObject + JSON export）

|フィールド|型|説明|
|---|---|---|
|`itemId`|string|英小文字snake_case、一意。例 `music_box`|
|`locKey`|string|`item.<id>.name`|
|`value`|int|売却額。0以上|
|`weightKg`|float|0.1〜40|
|`sizeClass`|enum|S/M/L/XL。見た目と収納不可判定|
|`fragility`|0..100|100で最も壊れやすい。落下/衝突で耐久減|
|`noiseRadius`|float|保持・落下時のGameplay Noise半径|
|`rarity`|enum|Common/Uncommon/Rare/Curse|
|`curseId`|nullable string|呪物固有挙動。通常品はnull|
|`twoHanded`|bool|保持中にライト以外のアクション不可|
|`requiredCarriers`|int|1/2。2は単独保持不可|
|`physicsProfile`|enum|Soft/Standard/Heavy/Delicate|
|`spawnWeight`|int|スポーン抽選重み|
|`durability`|int|0で破損。破損時の価値は `brokenValueMultiplier`|
|`throwable`|bool|投擲可否|
|`carryNoiseRadius`|float|運搬中の連続ノイズ半径|

### 他の必須定義

`DebuffDefinition { id, locKey, incompatibleIds[], parameters, soloFallback }`、`EnemyDefinition { id, hp:none, speed*, sense*, stateDurations }`、`SpawnPoint { id, category, enabled, weight }`、`RunConfig { timeLimit, targetValue, itemCounts, enemyCounts }`、`GraphicsPreset { all graphics fields }`。ネットワークIDはNGOの `NetworkObjectId` を正とし、DataのIDと混同しない。

## 8. プロトタイプ用アイテム

### 通常品

|ID / 名称|価値|kg/Size|壊れ|Noise|特性|SpawnWeight|
|---|---:|---|---:|---:|---|---:|
|`plush_bear` ぬいぐるみ熊|80|0.7/S|5|0|投擲可|18|
|`toy_car` ブリキの車|110|1.2/S|35|3|落下で走行音|16|
|`music_box` オルゴール|180|1.5/S|75|5|保持中4秒ごとに2m音|12|
|`dollhouse` ドールハウス|260|5/M|55|4|両手|10|
|`robot` 大型ロボット|330|9/L|20|5|両手|8|
|`glass_unicorn` ガラスユニコーン|420|2/M|95|7|破損時価値10%|6|
|`train_set` 鉄道セット|500|12/L|45|6|両手|5|
|`giant_block` 巨大積み木|620|22/XL|10|7|2人必須|4|
|`display_case` 展示ケース|720|28/XL|65|8|2人必須、破損時30%|3|
|`coin_capsule` 限定コイン|950|0.4/S|20|1|Rare|2|

### 呪物

|ID / 名称|価値|kg/Size|壊れ|Noise|特殊効果|SpawnWeight|
|---|---:|---|---:|---:|---|---:|
|`cursed_doll` 呪いの人形|800|1.5/S|0|4|保持者の位置をコレクターが12秒ごとに調査。置くと停止|3|
|`mirror_box` 鏡箱|1,000|7/M|60|2|ライト照射中はマネキンを4秒停止。保持者は`TunnelVision`扱い|2|
|`windup_clown` ゼンマイ道化師|1,200|3/M|40|10|20秒ごとに10m Noise。2人で運ぶとNoise 5m|2|
|`black_blocks` 黒い積み木|1,500|24/XL|15|8|2人必須。搬送中、近い敵を3秒ごとにPingする代わりに敵速度+10%|1|

## 9. 敵AI（HostのみがNavMesh/状態機械を実行、Clientsは状態を受信）

### 共通知覚

- 視覚：Hostからレイキャスト。FOV内、遮蔽物なし、照度係数を掛ける。Laptopでも**同一のWorld判定**。
- 聴覚：`GameplayNoiseEvent(position, radius, type)` をHostに送る。可聴Audioの波形や音量を使わない。
- ターゲット：`視認中の生存者 > 最後に聞いたNoise > 呪物 > 最近のPing`。Downedは近距離のみ優先。
- 攻撃：射程1.3m、クールダウン2.0秒。攻撃判定はHost。

### マネキン `mannequin`

|State|遷移/挙動|
|---|---|
|Idle|初期位置で静止、20秒。視認/NoiseでDetect|
|Patrol|固定Waypointを1.8m/sで巡回。10秒に一度2秒停止|
|Detect|対象を0.4秒確認。視覚を失えばInvestigate|
|Freeze|誰かのライト中心に2秒以上照射される間、移動0。視線だけ回す。`mirror_box`は4秒|
|Investigate|Noise地点へ3.2m/s、6秒探索。発見でChase、失敗Return|
|Chase|4.6m/s、視認を失うと最後の位置へ8秒追跡|
|Attack|1.3m内、0.4秒予備→Downed。終了後1.0秒硬直|
|Return|最寄りWaypointへ2.2m/s、Patrolへ|

視覚：12m、FOV90°、暗所では7m。聴覚：最大10m。アイテム反応：落下/運搬NoiseをInvestigate、呪物には反応しない。ライトが全プレイヤーに有効な止め方であり、無限拘束を防ぐため同一ライト照射は最長6秒、その後4秒耐性。

### コレクター `collector`

|State|遷移/挙動|
|---|---|
|Idle|スポーン室で待機。売却済み額が$500または呪物NoiseでHunt|
|Hunt|価値$300以上の未売却アイテム/呪物を評価し、最も近いものへ3.4m/s|
|InspectItem|アイテム1m内で1.2秒。プレイヤーを視認/NoiseでInvestigate|
|Steal|対象アイテムをKinematic所有しSpawn棚へ持ち去る。到達で再スポーン扱い|
|Investigate|Noise/最後のプレイヤー位置へ4.0m/s、7秒|
|Chase|5.0m/s、持ち運び中のプレイヤーを優先。8秒視認なしでReturn|
|Attack|マネキン同様だがCD2.5秒|
|Return|Spawn棚へ3.0m/s、Idle|

視覚：10m、FOV100°。聴覚：14m。ライトで止まらないが、2人のライトが同時に当たると2秒だけ`Stagger`（CD12秒）。アイテムを盗むことで「早く運ぶ/守る」判断を作る。

## 10. ステージ「閉店した巨大玩具店」

- **推奨面積**：80m × 65m（約5,200m²）、プレイ可能室11、廊下/倉庫含む。NavMeshは1フロアのみ。
- **Spawn**：入口バックヤード（安全地帯、売却カート、出口ゲート）に全員スポーン。開始30秒は敵侵入不可。
- **出口**：入口横のシャッター。目標額までは施錠。開放後、3秒滞在で脱出。
- **安全地帯**：バックヤード18m×12mのみ。アイテムは置けるが、売却はカート半径2m。敵は入れないが、時間停止/回復はしない。
- **部屋**：入口/レジ、ぬいぐるみ売場、知育玩具、模型売場、ドールハウス、ゲーム売場、展示通路、倉庫A/B、事務室、修理工房。
- **Spawn Point**：通常30、Rare6、Curse4。1ランで18〜20地点を使用。重品は倉庫/展示通路のみ。
- **Enemy Spawn**：マネキンは展示通路、コレクターは倉庫B。開始時は片方のみ、目標$500で2体目を有効化。
- **ギミック**：手動シャッター2枚（ドア保持）、非常灯スイッチ3、狭い棚通路、壊れやすい展示棚、移動式台車（見た目のみ、物理車両は作らない）。
- **照明**：全体は薄暗い。重要経路には非常灯。暗部でも最低限の輪郭を確保し、描画設定で隠れ場所の実判定を変えない。
- **隠れ場所**：棚裏6、段ボール山4、カウンター下3。Hostにより隠れVolume内 + しゃがみ + 敵との遮蔽で成立。
- **ランドマーク**：入口の赤いロケット、中央の巨大クマ、青い模型列車、天井の月型看板。HUD地図は作らず、ランドマークで会話させる。

## 11. Multiplayer設計

### 方針

**Host Authority** を採用する。試作は友人間の2〜4人プレイであり、専用サーバー運用よりセットアップ速度を優先する。Hostがゲーム状態・AI・扉・アイテムの最終決定者になる。クライアント権威を禁止する。Host migration/途中参加はFuture。

|対象|Authority / 送信|頻度・方式|
|---|---|---|
|Player|各Client入力→Host検証→NetworkTransform|入力20Hz、補間。位置の直接書換え禁止|
|Rigidbody Item|Host所有|動作中20Hz、睡眠中は状態変更時のみ。近接クライアントにのみ可視化しても判定はHost|
|共同運搬|Hostの`CarrySession`|運搬者ID/ItemID/状態をReliable、アイテム座標20Hz|
|Enemy AI|Host|State/位置10Hz、アニメはClient補間|
|Door|Host|開度/Lock/Heldを状態変更時＋開閉中10Hz|
|Curse|Host|効果開始/停止をReliable、周期イベントはHost生成|
|Debuff|Host|ラン開始時Reliable、状態値変更時Reliable|
|Death|Host|状態遷移Reliable、死体位置はHost追従|
|Stage Result|Host|売却額・タイマー・フェーズをReliable。クライアント集計禁止|

### 同期規則

- RPCは「要求」と「確定通知」を分ける。例：`RequestInteractServerRpc` → Host検証 → `CarryStartedClientRpc`。
- 客観状態はNetworkVariable/NetworkList、瞬間イベントはClientRpc。Clientのローカル予測は移動だけ。アイテム/敵を予測しない。
- RigidbodyはNetworkRigidbodyを使わず、Hostだけ物理演算。共同運搬・盗難中はKinematic化。
- ネットワーク更新の距離最適化（interest management）はFuture。ただし試作でも睡眠アイテムは送信停止。

## 12. 切断・途中退出（Vertical Sliceまで）

|事象|処理|
|---|---|
|Client切断|0.5秒後、当人をDead扱い。保持アイテムは最後の確定位置に落とす。共同運搬は即解除しアイテムを床へ。デバフは消える。死体があれば残す|
|Client再接続|非対応。ロビーへ戻す|
|共同運搬者切断|残った人は運搬解除、アイテムは0.2秒後に重力ON。貫通防止の位置補正|
|Host切断|全クライアントに「Host disconnected」、5秒後ロビーへ。ランは無効、設定は保存| 
|途中退出|PauseからLeave。上と同じClient切断処理|

## 13. Graphics / Laptop Mode

### プリセット

|項目|Laptop|Low|Medium|High|
|---|---|---|---|---|
|基準解像度/Render Scale|1280×720 / 70%|1920×1080 / 75%|1920×1080 / 100%|2560×1440 / 100%|
|FPS目標|30|45|60|60+|
|Shadow|Off（敵/扉判定は不変）|Low 20m|Medium 40m|High 70m|
|Lighting|Bakedのみ+重要ライト8|Baked+8|Baked+16|Baked+24|
|Reflection|Off|Probe|Probe|Probe+SSR（任意）|
|Fog|簡略距離Fog|簡略|Volumetric Low|Volumetric Med|
|Post Process|Bloom/DOF/MB/Grain Off|Bloom Low|Bloom/AO Low|Bloom/AO Med、DOF/MB Off既定|
|Particles|25%|50%|75%|100%|
|Decals|Off|16|32|64|
|Texture|512〜1024|1024|2048|2048〜4096|
|LOD|LOD2優先|LOD1|LOD1|LOD0近距離|
|Physics|物理Hz 30、アイテム最大24|40、36|50、48|50、64|
|Animation|遠方10Hz|15Hz|20Hz|30Hz|
|AI Update|遠方5Hz/近方10Hz|同左|同左|同左|

Laptop ModeはIntel Iris Xe/Intel Graphics/AMD iGPU/共有VRAMで**720p相当・30FPS**を目標とする。敵の視覚、Noise、隠れVolume、アイテムSpawn、扉衝突、ライトのゲーム判定、表示距離に関わる敵シルエットはHighと一致させる。影を切ることで敵が見えやすくなる等の優位を作らない。必要な敵輪郭は全モードで同じ最小距離まで描画する。

### 設定画面仕様

- Preset：Laptop/Low/Medium/High/Custom。個別変更時はCustomに遷移。
- 項目：Resolution、Display Mode、VSync、FPS Limit、Render Scale、Texture、Shadow、Shadow Distance、AA、AO、Reflection、Fog、Bloom、DOF、Motion Blur、Film Grain、Particles、Effects、Decals、View Distance、Brightness、FOV（75〜100）。
- `Apply` は一時適用、`Revert` はApply前のセッション値へ、`Reset Graphics` は保存済みのGraphicsキーを削除してMedium初期値へ即時適用。別ボタンであり、画面に常時表示する。
- Resolution/Display Mode変更時は15秒の「変更を保持」ダイアログ。Confirmされなければ旧値に自動復帰。クラッシュ/設定ファイル不正時は1280×720 Windowed, Low, VSync On, Render Scale 70%で起動し警告を出す。

## 14. Localization

- Unity Localization package。すべての表示文字列はString Table参照。コード/UI Prefabに日本語リテラルを置かない（開発ログ除く）。
- Key：`<screen>.<element>.<purpose>`、例 `hud.objective.target_value`、`item.music_box.name`、`debuff.tremor.description`。英小文字snake_case。
- Locale：`ja-JP`（既定）→ `en-US` → `zh-Hans` → `ko-KR`。指定言語にKeyがなければen-US、さらに無ければ `[MISSING:key]` を開発ビルドで表示、製品ビルドではja-JP。
- Font：日本語/中国語/韓国語を同一TMP Font Assetまたは各Locale fallback chainで完全収録。CJKを画像化しない。
- UIは固定文字幅禁止。横幅最大、折返し、Auto-size最小値、スクロールを設計。翻訳文は最大1.6倍（CJKは1.3倍）を許容。
- 未翻訳検出：CI/Editor testで全必須Keyの4Locale充足を検査。`[MISSING]` は出荷ブロッカー。

## 15. UI / UX情報構造

|画面|必須構成|
|---|---|
|Main Menu|Play、Settings、Quit、右下にVersion/Language|
|Lobby|Host/Join（IP/Join Codeは開発用）、Player一覧、Ready、開始、選択中のMap、接続状態。HostのみStart|
|Settings|Graphics / Audio / Controls / Language のタブ、Apply/Revert/Back|
|Graphics|Preset、個別項目、右列にApply/Revert、下部に常時 `Reset Graphics`|
|Audio|Master/BGM/SFX/Ambient/Voice（Voiceは将来機能でもスライダーは保存しない）、Mute、テスト音|
|Controls|Action名/現在のキー/Rebind/Reset Controls。競合時は確認|
|Language|4言語リスト、即時プレビュー、Applyで保存|
|Gameplay HUD|左上：残時間/目標$・売却$。下中央：Interact。右下：スタミナ・ライト。中央下：所持品。上中央：Ping/救助。画面端：デバフアイコン+短文|
|Pause|Resume、Settings、Leave Lobby。HostのPauseはローカル停止せず、Hostだけ操作停止（ネットランは継続）|
|Result|成功/失敗、目標対比、内訳、脱出/死亡/回収、デバフ補助数、Retry Lobby|

## 16. Audio

- Mixer group：Master / BGM / SFX / Ambient / Voice。0〜100、dB変換、設定に保存。Voiceはゲーム内ボイチャを実装しないため予約枠のみ。
- `AudibleAudio` と `GameplayNoise` は完全分離。SFXを鳴らしてもAIには自動で聞こえない。AI反応が必要な行動だけがHostにNoise Eventを発行する。
- Noise種別：Footstep（歩行1.5m/ダッシュ4m）、Drop（item noise）、Throw、HeavyBreath、Curse、Door、Enemy。可視デバッグ円を開発ビルドで用意する。

## 17. Save

- 保存：Language、Graphics、Audio、Input bindings、最後に選んだPreset。JSON+PlayerPrefs等の軽量ローカル保存でよい。
- 保存しない：ラン進行、所持品、実績、デバフ、ネット接続、リザルト履歴。ゲーム進行セーブはプロトタイプでは不要であり実装しない。
- 読み込みはschemaVersionを持つ。失敗時は設定のみ安全既定値へ復帰。

## 18. パフォーマンスBudget（目安）

|指標|Laptop|Low|Medium|High|
|---|---:|---:|---:|---:|
|Draw calls|≤500|≤800|≤1200|≤1800|
|Active Rigidbody|≤24|≤36|≤48|≤64|
|リアルタイムライト|≤8|≤8|≤16|≤24|
|影付きライト|0|≤1|≤2|≤4|
|同時Particle|≤80|≤150|≤300|≤500|
|敵AI|2（近方10Hz/遠方5Hz）|同左|同左|同左|
|Network send|Client平均≤20KB/s、Host平均≤80KB/s|同左|同左|同左|

Profiler上で30秒の4人ランを計測する。Laptopは30FPS未満が連続3秒、Highは60FPS未満が連続3秒で失敗として最適化チケットを作る（未最適化アセットを除く）。

## 19. Acceptance Criteria

### コア

- Given 2人が`giant_block`の2m以内にいる / When 両者がE / Then Hostが両者を検証し、2人の中点にアイテムをKinematic追従させ、単独保持できない。
- Given `Tremor`の人が`glass_unicorn`を単独保持 / When 1秒歩く / Then 通常より大きい保持揺れが発生する。2人運搬開始後は揺れが規定値以下になる。
- Given 売却額$1,500 / When カートにアイテムを入れる / Then Hostの売却額だけが更新され、出口が解錠される。
- Given 目標達成済みの生存者 / When 出口内に3秒滞在 / Then その人はEscapedとなり再入場できない。
- Given 生存者が0 / When 最後の人がDead / Then 5秒以内に失敗Resultへ遷移し未売却品は加算されない。

### ネットワーク/敵/設定

- Given HostとClient / When ClientがInteract要求を送る / Then Hostが距離/状態を検証し、不正な要求では状態を変えない。
- Given マネキンがライトを2秒以上受ける / When Freeze条件を満たす / Then 全Clientで停止状態になり、6秒後は4秒耐性が働く。
- Given Clientが共同運搬中に切断 / When 切断を検知 / Then 0.5秒以内に運搬解除、アイテムはHost位置で落下する。
- Given Graphics画面 / When `Reset Graphics` / Then 保存値を含めMedium初期値へ戻り、再起動後もその値である。
- Given Fullscreen解像度変更 / When 15秒以内にConfirmしない / Then 以前の解像度/表示モードに戻る。
- Given 4Locale / When Main MenuとHUDを開く / Then `[MISSING:` が表示されず、文字が切れない。

## 20. テスト計画

|区分|ケース|合格基準|
|---|---|---|
|Solo|各デバフで開始〜脱出|代替手段で詰まず、12分以内にクリア可能|
|2 Players|共同運搬、救助、受け渡し|双方の表示/所有/結果が一致|
|4 Players|全デバフ、敵2、売却|60秒以上の位置ズレ/アイテム複製なし|
|Host/Client|Host/Client各操作|Host検証外の状態変更なし|
|High/Laptop|同一ラン|ゲーム判定一致、各FPS目標達成|
|言語|ja/en/zh-Hans/ko|欠損/重なり/切りがない|
|Disconnect|保持、共同運搬、Downed、Host|第12章どおり、クラッシュなし|
|Death|救助成功/中断/タイムアウト|状態とアイテムが全員一致|
|Item Physics|投擲、落下、破損、睡眠|価値計算とNoiseが一回だけ発生|
|Debuff|10種×ソロ/協力|相互補完の操作が成立、過度な入力阻害なし|
|Enemy|各State遷移|HostだけがAIを決定し、Client表示が追従|
|Git|Phase branch / Commit / Merge|未追跡の重要ファイルなし、秘密情報なし、mainは常時Build可能、各Phaseのcommit hashを追跡可能|

## 21. リスク

|Risk|Impact|対策|
|---|---|---|
|Multiplayer Physics|高|Host物理のみ。重要物のみ同期、共同運搬Kinematic化|
|Rigidbody同期|高|睡眠時送信停止、最大数Budget、NetworkTransformの閾値/補間を調整|
|Physics Jitter|中|物理Hz固定、Host座標を正、Client予測をアイテムに使わない|
|Latency|中|InteractをHost検証、開始中の明確なPending表示、移動のみ局所予測|
|Laptop Mode|高|Baked光源中心、描画とゲーム判定分離、毎PhaseでIris級プロファイル|
|Localization|中|Key検査、4言語のスクリーン比較、CJK Fontを早期導入|
|AI負荷|中|敵2体上限、視覚/聴覚を低頻度化、HostだけNavMesh計算|
|Scope Creep|高|MVP/VS/Futureタグ、未記載実装禁止、Phase完了前の新機能禁止|
|Git履歴破損/巨大Asset|中|履歴破壊操作禁止、Unity Force Text/Visible Meta Files、Git LFS、Phase branch、commit前diff確認|


## 22. Git管理仕様

### 22.1 基本方針

- 本プロジェクトは**Gitを必須のバージョン管理手段**とする。GitHub等のRemote hostingは必須ではない。
- `main` は常に「Windows Standalone Buildが通り、Console error 0で、直近PhaseのAcceptance Criteriaを満たす状態」を維持する。
- Codexは原則として `main` へ直接実装しない。各Phaseまたは独立Featureごとにbranchを作成する。
- Remoteが設定されている場合でも、Codexは明示的な指示なしに`push --force`、Remote branch削除、Release作成を行わない。
- Gitの履歴は、バグ調査・仕様差分確認・安全なロールバックに使用する。コミットを「作業ログ」ではなく「再現可能な変更単位」として扱う。

### 22.2 Unity側の必須設定

Unity Editorでは以下を固定する。

- Version Control Mode：`Visible Meta Files`
- Asset Serialization Mode：`Force Text`
- `.meta` ファイルは対応Assetと必ず同時にGit管理する。
- `.unity` / `.prefab` / `.asset` / ProjectSettingsはテキスト差分を維持し、理由なくバイナリ化しない。
- `ProjectSettings/` と `Packages/manifest.json` / `Packages/packages-lock.json` はGit管理対象とする。
- 個人環境依存のEditor設定や生成物はGit管理しない。

### 22.3 Repository初期構成

新規Repositoryでは最低限以下を用意する。

```text
/
├─ Assets/
├─ Packages/
├─ ProjectSettings/
├─ .gitignore
├─ .gitattributes
├─ README.md
├─ CHANGELOG.md
└─ Docs/
   └─ Specs/
      └─ 3D協力型ホラーゲーム_実装仕様書.md
```

本仕様書もRepository内の `Docs/Specs/` に保存し、仕様変更をGit差分で追跡できるようにする。

### 22.4 `.gitignore`

Unity向けの`.gitignore`をRepository Rootに置く。

最低限、以下を追跡対象外とする。

```gitignore
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Ll]ogs/
/UserSettings/
/MemoryCaptures/
/Recordings/
.vs/
.idea/
.vscode/
*.csproj
*.sln
*.suo
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db
.DS_Store
Thumbs.db
```

ただし`.vscode/`等をチーム共通設定として意図的に管理する場合は、必要ファイルのみ例外指定してよい。

### 22.5 Git LFS

大容量バイナリアセットはGit LFSを使用する。

初期対象候補：

```gitattributes
*.psd filter=lfs diff=lfs merge=lfs -text
*.blend filter=lfs diff=lfs merge=lfs -text
*.fbx filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
```

以下は原則通常Gitで管理する。

- `.cs`
- `.json`
- `.csv`
- `.md`
- `.unity`
- `.prefab`
- `.asset`
- `.meta`
- ProjectSettings

PNG/JPGなどは実ファイルサイズを確認し、無条件にLFSへ入れない。巨大なテクスチャ素材のみ必要に応じてLFS対象へ追加する。

### 22.6 Branch規約

基本branch：

```text
main
```

Phase branch：

```text
feature/phase-01-foundation
feature/phase-02-network-coop
feature/phase-03-debuffs
feature/phase-04-enemies-noise
feature/phase-05-toy-store-slice
feature/phase-06-ui-localization-graphics
feature/phase-07-qa
```

独立した修正：

```text
fix/<short-description>
```

小規模な技術改善：

```text
refactor/<short-description>
chore/<short-description>
```

Phaseをまたぐ巨大branchは作らない。

### 22.7 Commit規約

Conventional Commitsに準じて以下を使用する。

```text
feat: ...
fix: ...
refactor: ...
test: ...
docs: ...
chore: ...
perf: ...
```

例：

```text
feat(items): add host-authoritative item pickup
feat(coop): implement two-player carry session
fix(network): release carried item on client disconnect
test(debuff): add tremor cooperation playmode tests
docs(spec): update laptop mode acceptance criteria
```

1コミットには1つの論理的変更だけを含める。

以下を1コミットへ混在させない。

- Feature追加
- 無関係なRefactor
- Formatterによる全ファイル変更
- Asset大量追加
- Bug Fix

Codexは`git add .`を機械的に使用せず、`git status` / `git diff`を確認し、自身が変更したファイルだけをStageする。

### 22.8 CodexのGit作業手順

各作業開始時：

1. `git status` を確認する。
2. 現在branchとHEADを確認する。
3. 既存の未コミット変更を確認する。
4. ユーザーまたは他作業者の未コミット変更を削除・上書きしない。
5. 対象Phaseのbranchがなければ作成する。
6. 既存branchがある場合は内容を確認して継続する。

実装中：

1. 小さな論理単位で変更する。
2. `git diff` を確認する。
3. Compile / Testを行う。
4. 成功した変更だけCommitする。
5. 失敗中の試行錯誤を無理にCommitしない。

Phase終了時：

1. `git status` を確認する。
2. Console error 0を確認する。
3. 指定Testを実行する。
4. Windows Standalone Buildを確認する。
5. Acceptance Criteriaを確認する。
6. 必要なDocs / CHANGELOGを更新する。
7. Phase完了Commitを作成する。
8. branch名、Commit hash、Test結果、未解決事項を報告する。

`main`への統合は、当該Phaseが合格した場合のみ行う。実行環境でmerge権限が明確でない場合、Codexは勝手にmergeせず、合格済みbranchとCommit hashを報告して停止する。

### 22.9 禁止Git操作

Codexは明示的な指示がない限り以下を行わない。

```text
git reset --hard
git clean -fd / -fdx
git checkout -- .
git restore .
git rebase --onto 等の履歴書換え
git commit --amend による既存履歴の変更
git push --force / --force-with-lease
mainの履歴書換え
Remote branch/tagの削除
```

問題が起きた場合は既存履歴を破壊せず、新しいFix commitで修正する。

### 22.10 Merge条件

Phase branchを`main`へ統合できる条件：

- Compile error 0
- Console error 0
- 対象PhaseのテストPASS
- Windows Standalone Build成功
- 対象Acceptance Criteria PASS
- 意図しないAsset/設定変更が`git diff`にない
- `.meta`欠落なし
- 秘密情報/API key/個人パスをCommitしていない

Merge後も`main`がビルドできることを再確認する。

### 22.11 Secret管理

以下をGitへCommitしない。

- API Key
- Access Token
- Password
- 個人アカウント情報
- Private certificate
- PC固有の絶対パス
- Remote Desktop / service credential

必要な値は環境変数、ローカル設定、`.env`等を使用し、`.env`は`.gitignore`へ追加する。

`.env.example`には値を入れず、必要Key名だけ記載してよい。

### 22.12 Tag / Milestone

以下のタイミングでAnnotated Tagを使用可能とする。

```text
prototype-v0.1-mvp
prototype-v0.2-vertical-slice
```

Tag作成は対応Acceptance Criteriaが完了し、`main`へ統合済みの場合のみ行う。

### 22.13 CHANGELOG

`CHANGELOG.md`にはCommit単位ではなく、Phase/Milestone単位で以下を記録する。

- Added
- Changed
- Fixed
- Known Issues

既知の不具合を隠さず記録する。


## 23. 開発Backlog（上から順に実施）

|Epic|Feature|Task|依存|
|---|---|---|---|
|E0 Foundation|Project|Unity 6 URP/NGO/Input/Localization導入、フォルダ規約、CIビルド|なし|
|E0 Foundation|Config|RunConfig、設定保存/安全起動、ログ|E0 Project|
|E1 Core|Player|FPS移動、状態機械、Interact、ライト、Ping|E0|
|E1 Core|World|灰箱マップ、Door、カート、出口、SpawnPoint|E1 Player|
|E1 Core|Items|ItemDefinition、保持/投擲/破損/売却|E1 Player/World|
|E2 Network|Session|Host/Join、Player同期、権威RPC|E1|
|E2 Network|Synced world|Item/Door/Result同期、切断|E2 Session|
|E3 Cooperation|Carry|共同運搬、受け渡し、救助、支える、ドア保持|E2|
|E3 Cooperation|Debuff|MVP4→VS10、ソロ代替、HUD|E3 Carry|
|E4 Threat|Noise|GameplayNoise debug/知覚|E2|
|E4 Threat|Enemies|マネキン→コレクター、NavMesh、同期|E4 Noise|
|E5 Slice|Map|玩具店アート/スポーン/隠れ場所|E1/E4|
|E5 Slice|Content|通常10/呪物4、リザルト|E3/E5 Map|
|E6 Polish|UX|4言語、設定画面、音量、アクセシビリティ|E0|
|E6 Polish|Performance|各Preset、Profiler、Laptop検証|E5/E6 UX|
|E7 QA|Acceptance|自動/手動テスト、バグ修正、ビルド|全Epic|

## 24. Codex Master Prompt

```text
あなたはUnity 6/C#の実装担当です。添付の「3D協力型ホラーゲーム 実装仕様書 v1.1（Git管理版）」を唯一の仕様として、Windows向けプロトタイプを実装してください。

目的は、デバフを持つ2〜4人が協力してアイテムを回収・売却し、敵から逃げるループが面白いかを検証することです。見た目や将来機能より、再現可能な協力プレイを優先してください。

技術固定：Unity 6 LTS / URP / C# / Input System / Netcode for GameObjects + Unity Transport / Unity Localization。ネットワークはHost Authority。HostのみがAI・物理・アイテム所有・売却・死亡・結果を決定し、Clientは要求を送るだけです。バージョン管理はGit必須で、mainは常時ビルド可能状態を維持し、Phase単位のfeature branchで作業してください。

実装順序：Phase Promptに従い、各Phaseを「Git状態確認→Phase branch作成/継続→実装→コンパイル→Play Mode/必要なら2インスタンス検証→エラー修正→git diff確認→論理単位でcommit→変更内容・commit hash・未解決点を報告」の順で完了してください。次Phaseの機能やFutureを先回りして実装しないでください。

禁止事項：仕様外のスキル/成長/プロシージャル生成/マッチメイク/ボイスチャット/永続進行を追加しない。`git reset --hard`、`git clean -fd`、`git restore .`、履歴書換え、force push、他者の未コミット変更の削除を行わない。ゲーム数値をコードに直書きしない。Client Authorityにしない。Rigidbody全量同期、アイテムのClient予測、ゲーム判定を描画品質へ依存させない。日本語文字列をPrefab/コードに直書きしない。未確認APIを仮定しない。

規約：DataはScriptableObject中心、IDはsnake_case、クラス/メソッドはPascalCase、public APIにはXMLコメント、1クラス1責務。CommitはConventional Commits形式とし、`git add .`を無確認で使用せず、自分の変更だけをStageする。Network RPCはRequestと確定通知を分ける。エラーを握りつぶさず、開発ビルドには分かるログを出す。既存ファイルを壊す変更前に確認し、差分を小さく保つ。

テスト：各PhaseでConsole error 0、Windows Standalone build成功、該当Acceptance Criteriaを実行して結果を記録する。ネットワーク機能はHost+Clientの2インスタンスで必ず確認する。性能はProfilerでBudgetに対し測定する。

完成条件：仕様書のVertical Slice範囲について、2〜4人で1ランを完走でき、共同運搬/デバフ補完/敵回避/売却/脱出/切断/4言語/Reset GraphicsがAcceptance Criteriaどおりに動くこと。
```

## 25. Phase別 Codex Prompt

### Phase 1 — 基盤とローカルMVP

```text
仕様書v1.1のE0〜E1を実装してください。Unity 6 URPプロジェクトにNGO/Input/Localizationの土台、設定の安全起動・Graphics Reset、ローカルFPS Player、Interact、ライト、Ping、灰箱マップ、Door、カート、出口、6通常アイテム、売却額/タイマー/Resultを作る。ネットワーク、敵、デバフはまだ作らない。ItemDefinition/RunConfigはData Drivenにする。Windows buildと1人で「回収→売却→目標→脱出」を検証し、結果を報告して止まる。


Git：作業開始時に`git status`を確認し、`feature/phase-01-foundation`を作成または継続する。既存の未コミット変更を破壊しない。テスト/ビルド合格後、変更を論理単位でConventional Commitsとしてcommitし、最終報告にbranch名とcommit hashを含める。mainへのmerge条件を満たさない状態ではmergeしない。
```

### Phase 2 — ネットワーク権威と共同運搬

```text
仕様書v1.1のE2〜E3（デバフ以外）を実装してください。NGOのHost/Client接続、Host AuthorityのPlayer/Item/Door/Stage Result同期、切断の最低限、2人共同運搬、受け渡し、救助、支える、ドア保持を追加する。Hostのみが状態を確定する。2インスタンスで共同運搬、救助、Client切断時のアイテム落下を検証し、Console error 0とWindows build成功後に報告して止まる。


Git：作業開始時に`git status`を確認し、`feature/phase-02-network-coop`を作成または継続する。既存の未コミット変更を破壊しない。テスト/ビルド合格後、変更を論理単位でConventional Commitsとしてcommitし、最終報告にbranch名とcommit hashを含める。mainへのmerge条件を満たさない状態ではmergeしない。
```

### Phase 3 — デバフと協力検証

```text
仕様書v1.1のデバフ仕様を実装してください。先にMVP4種（Tremor/TunnelVision/HeavyBreath/FragileGrip）、次に残り6種をData Drivenに追加する。各人1個、禁止組合せ、ソロ代替、仲間の操作での補完、HUD/Localizationを実装する。単なる能力低下にせず、指定された協力アクションによって緩和されることを確認する。Soloと2人で全デバフのAcceptanceを検証して報告して止まる。


Git：作業開始時に`git status`を確認し、`feature/phase-03-debuffs`を作成または継続する。既存の未コミット変更を破壊しない。テスト/ビルド合格後、変更を論理単位でConventional Commitsとしてcommitし、最終報告にbranch名とcommit hashを含める。mainへのmerge条件を満たさない状態ではmergeしない。
```

### Phase 4 — 敵とノイズ

```text
仕様書v1.1のAudio/Noiseと敵AIを実装してください。可聴AudioとGameplayNoiseを分離し、Hostのみが知覚とState Machineを実行する。マネキン全Stateと、次にコレクター全Stateを追加する。ライト/呪物/アイテムへの反応を仕様通りにする。2インスタンスでState遷移、ライト停止、アイテム盗難、死亡/救助を確認して報告して止まる。


Git：作業開始時に`git status`を確認し、`feature/phase-04-enemies-noise`を作成または継続する。既存の未コミット変更を破壊しない。テスト/ビルド合格後、変更を論理単位でConventional Commitsとしてcommitし、最終報告にbranch名とcommit hashを含める。mainへのmerge条件を満たさない状態ではmergeしない。
```

### Phase 5 — 玩具店Vertical Slice

```text
仕様書v1.1のステージ・コンテンツ範囲を実装してください。固定の閉店玩具店を11エリアで構成し、指定ランドマーク、隠れVolume、Spawn Point、通常10/呪物4、ランダム配置、敵の段階解放、リザルト内訳を追加する。完全自動生成・追加マップは作らない。4人で12分ランを通し、目標額・脱出・敵・デバフ補完が成立することを検証して報告して止まる。


Git：作業開始時に`git status`を確認し、`feature/phase-05-toy-store-slice`を作成または継続する。既存の未コミット変更を破壊しない。テスト/ビルド合格後、変更を論理単位でConventional Commitsとしてcommitし、最終報告にbranch名とcommit hashを含める。mainへのmerge条件を満たさない状態ではmergeしない。
```

### Phase 6 — UI、言語、Graphics、性能

```text
仕様書v1.1のUI/UX、Localization、Graphics/Laptop Modeを仕上げてください。全画面、4Locale、Font fallback、欠損Key検査、Audio設定、Preset、Apply/Revert/常時表示のReset Graphics、15秒解像度復帰、安全起動を実装する。Laptop/Low/Medium/HighをProfilerで測定し、ゲーム判定が同一であることを確認する。各言語・High・Laptopでビルド/テスト後に報告して止まる。


Git：作業開始時に`git status`を確認し、`feature/phase-06-ui-localization-graphics`を作成または継続する。既存の未コミット変更を破壊しない。テスト/ビルド合格後、変更を論理単位でConventional Commitsとしてcommitし、最終報告にbranch名とcommit hashを含める。mainへのmerge条件を満たさない状態ではmergeしない。
```

### Phase 7 — QAと受入

```text
仕様書v1.1のAcceptance CriteriaとTest Planを実施し、未達を修正してください。Solo/2/4人、Host/Client、4言語、High/Laptop、切断、死亡、物理、共同運搬、全デバフ、両敵を網羅する。結果をPASS/FAIL/既知制約の表で出し、FAILは修正して再試験する。Future機能は追加しない。Windows最終ビルド成功とConsole error 0を確認して完了報告する。


Git：作業開始時に`git status`を確認し、`feature/phase-07-qa`を作成または継続する。既存の未コミット変更を破壊しない。テスト/ビルド合格後、変更を論理単位でConventional Commitsとしてcommitし、最終報告にbranch名とcommit hashを含める。mainへのmerge条件を満たさない状態ではmergeしない。
```

## 26. 実装開始時の判断順

1. 本文の「固定する前提」→数値表→Acceptance Criteriaの順で解釈する。
2. 競合時は、協力行動を増やしつつ操作不能を減らす方を選ぶ。
3. ネットワーク/物理で迷ったらHost権威・Kinematic共同運搬・低頻度同期を選ぶ。
4. Scopeにない機能が必要に見えたら実装せず、ブロッカーとして報告する。
5. 作業開始時とCommit前に`git status` / `git diff`を確認し、ユーザーや他作業者の未コミット変更を破壊しない。
